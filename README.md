# quick_sftp
quick_sftp is a lightweight, hardened Python SFTP server for quickly sharing a local directory over SSH/SFTP with safe defaults, public-key authentication, optional password auth, read-only mode, and LAN-access guardrails.
